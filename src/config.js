export const API_BASE = `${import.meta.env.VITE_API_URL || ''}/api`;

// Canonical production URL — used for absolute links in structured data (schema.org).
export const SITE_URL = 'https://www.cravebetter4u.com';
